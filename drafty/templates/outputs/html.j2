<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ metadata.title | default('Article') }}</title>
    <meta name="description" content="{{ metadata.description | default('') }}">
    <meta name="author" content="{{ metadata.author | default('Drafty AI') }}">
    <meta name="date" content="{{ metadata.date }}">
    {% if config.content.keywords %}
    <meta name="keywords" content="{{ config.content.keywords | join(', ') }}">
    {% endif %}
    
    {% if include_styles %}
    <style>
        :root {
            --primary-color: #2563eb;
            --text-color: #1f2937;
            --bg-color: #ffffff;
            --code-bg: #f3f4f6;
            --border-color: #e5e7eb;
            --quote-color: #6b7280;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.7;
            color: var(--text-color);
            background: var(--bg-color);
        }
        
        .container {
            max-width: 720px;
            margin: 0 auto;
            padding: 2rem 1.5rem;
        }
        
        header {
            margin-bottom: 3rem;
            padding-bottom: 2rem;
            border-bottom: 2px solid var(--border-color);
        }
        
        .meta {
            display: flex;
            gap: 2rem;
            margin-top: 1rem;
            font-size: 0.875rem;
            color: var(--quote-color);
        }
        
        h1 {
            font-size: 2.5rem;
            line-height: 1.2;
            font-weight: 700;
            color: var(--text-color);
            margin-bottom: 0.5rem;
        }
        
        h2 {
            font-size: 1.875rem;
            font-weight: 600;
            margin-top: 2.5rem;
            margin-bottom: 1rem;
            color: var(--text-color);
        }
        
        h3 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-top: 2rem;
            margin-bottom: 0.75rem;
        }
        
        h4, h5, h6 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-top: 1.5rem;
            margin-bottom: 0.5rem;
        }
        
        p {
            margin-bottom: 1.25rem;
        }
        
        a {
            color: var(--primary-color);
            text-decoration: none;
            border-bottom: 1px solid transparent;
            transition: border-color 0.2s;
        }
        
        a:hover {
            border-bottom-color: var(--primary-color);
        }
        
        ul, ol {
            margin-bottom: 1.25rem;
            padding-left: 1.75rem;
        }
        
        li {
            margin-bottom: 0.5rem;
        }
        
        blockquote {
            margin: 1.5rem 0;
            padding: 1rem 1.5rem;
            border-left: 4px solid var(--primary-color);
            background: var(--code-bg);
            font-style: italic;
            color: var(--quote-color);
        }
        
        code {
            background: var(--code-bg);
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
            font-size: 0.875rem;
            font-family: 'Courier New', monospace;
        }
        
        pre {
            background: var(--code-bg);
            padding: 1.25rem;
            border-radius: 0.5rem;
            overflow-x: auto;
            margin-bottom: 1.25rem;
        }
        
        pre code {
            background: none;
            padding: 0;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 1.5rem 0;
        }
        
        th, td {
            padding: 0.75rem;
            text-align: left;
            border: 1px solid var(--border-color);
        }
        
        th {
            background: var(--code-bg);
            font-weight: 600;
        }
        
        hr {
            margin: 2rem 0;
            border: none;
            border-top: 2px solid var(--border-color);
        }
        
        footer {
            margin-top: 4rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border-color);
            font-size: 0.875rem;
            color: var(--quote-color);
            text-align: center;
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 1.5rem 1rem;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            h2 {
                font-size: 1.5rem;
            }
        }
        
        @media print {
            body {
                font-size: 12pt;
            }
            
            .container {
                max-width: 100%;
            }
            
            a {
                color: inherit;
                text-decoration: underline;
            }
        }
    </style>
    {% endif %}
</head>
<body>
    <div class="container">
        <header>
            <h1>{{ metadata.title | default(config.content.topic) }}</h1>
            {% if metadata.description %}
            <p class="description">{{ metadata.description }}</p>
            {% endif %}
            <div class="meta">
                <span>By {{ metadata.author | default('Drafty AI') }}</span>
                <span>{{ metadata.date }}</span>
                {% if config.content.word_count %}
                <span>{{ content | wordcount }} words</span>
                {% endif %}
            </div>
        </header>
        
        <article>
            {{ content | safe }}
        </article>
        
        <footer>
            <p>Generated with <a href="https://github.com/yourusername/drafty">Drafty AI</a></p>
            <p><small>&copy; {{ metadata.date[:4] }} {{ metadata.author | default('Drafty AI') }}. All rights reserved.</small></p>
        </footer>
    </div>
</body>
</html>