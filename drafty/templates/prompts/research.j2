You are a research assistant helping to gather information for an article about "{{ topic }}".

Target Audience: {{ audience }}

{% if queries %}
Research Queries:
{% for query in queries %}
- {{ query }}
{% endfor %}
{% endif %}

{% if sources %}
Existing Sources:
{% for source in sources %}
- {{ source.title }} ({{ source.url }})
{% endfor %}
{% endif %}

Please analyze the topic and provide:

1. **Key Concepts**: Identify the main concepts and themes that should be covered
2. **Important Questions**: What questions would the {{ audience }} want answered?
3. **Research Gaps**: What information is missing that we should investigate?
4. **Recommended Sources**: Suggest types of sources that would be valuable
5. **Search Queries**: Provide 5-10 specific search queries to find relevant information

Focus on finding authoritative, recent, and relevant information that would be valuable for {{ audience }}.

Respond in JSON format with the following structure:
{
  "key_concepts": ["concept1", "concept2", ...],
  "important_questions": ["question1", "question2", ...],
  "research_gaps": ["gap1", "gap2", ...],
  "recommended_source_types": ["type1", "type2", ...],
  "search_queries": ["query1", "query2", ...]
}